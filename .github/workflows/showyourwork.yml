name: article

on: push

jobs:
  showyourwork:
    runs-on: ubuntu-latest
    name: Build the article PDF
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Build the article PDF
        id: build
        uses: rodluger/showyourwork/showyourwork-action@main
